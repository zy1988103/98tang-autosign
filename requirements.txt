# 核心依赖
selenium>=4.15.0
python-dotenv>=1.0.0
numpy>=1.24.0
requests>=2.31.0

# 增强反检测（可选但推荐）
undetected-chromedriver>=3.5.5
setuptools  # 解决Python 3.12+的distutils问题

# Chrome驱动自动管理（可选）
webdriver-manager>=4.0.0